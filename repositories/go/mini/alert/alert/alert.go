package alert

// Alert is a struct that represents an alert in the system.
type Alert struct {
}

// Rule is a struct that represents a rule for generating alerts.
type Rule struct {
}

// Event is a struct that represents an event in the system.
type Event struct {
}
